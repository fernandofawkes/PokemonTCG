import { DetectVisibilityDirective } from './detect-visibility.directive';

describe('DetectVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
